apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: team-onboarding-template
  description: Creates a new team namespace with RBAC, quotas, network policies, and observability
spec:
  targets:
    - ./template.yaml
